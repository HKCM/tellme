---
tags: ["遍历文件夹","文件夹遍历","文件遍历"]
---
# 遍历文件
---
## Example1
```go
func visit(files *[]string) filepath.WalkFunc {
    return func(path string, info os.FileInfo, err error) error {
        if err != nil {
            log.Fatal(err)
        }
        *files = append(*files, path)
        return nil
    }
}

func main() {
    var files []string

    root := "/some/folder/to/scan"
    // 会扫描所有子文件夹
    err := filepath.Walk(root, visit(&files))
    if err != nil {
        panic(err)
    }
    for _, file := range files {
        fmt.Println(file)
    }
}
```

## Example2
```go
fs, err := os.ReadDir(dirPath)
if err != nil {
	panic(err)
}
for _, f := range fs {
	if f.IsDir() {
		fmt.Println(f.Name()) // 只显示目录名字
	}
}
```
---
